services:
  postgres:
    image: pgvector/pgvector:pg15
    environment:
      POSTGRES_USER: next_hub_user
      POSTGRES_PASSWORD: next_hub_password
      POSTGRES_DB: next_hub_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
